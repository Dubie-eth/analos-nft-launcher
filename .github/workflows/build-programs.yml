name: Build Solana Programs

on:
  workflow_dispatch:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Remove incompatible Cargo.lock
      run: |
        find . -name "Cargo.lock" -type f -delete
        echo "Removed all Cargo.lock files"
    
    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libudev-dev pkg-config libssl-dev
    
    - name: Install Solana
      run: |
        wget -q -O - https://github.com/solana-labs/solana/releases/download/v1.18.17/solana-release-x86_64-unknown-linux-gnu.tar.bz2 | tar jxf -
        mkdir -p $HOME/.local/share/solana/install/active_release
        cp -r solana-release/* $HOME/.local/share/solana/install/active_release/
        echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
    
    - name: Build Programs
      run: |
        export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
        solana --version
        cargo --version
        echo "Removing ALL Cargo.lock files..."
        find . -name "Cargo.lock" -type f -exec rm -fv {} \;
        echo "Building programs with Solana's toolchain..."
        cd programs/analos-nft-launchpad && cargo build-sbf && cd ../..
        cd programs/analos-token-lock-enhanced && cargo build-sbf && cd ../..
        echo "Build complete!"
        ls -la target/deploy/
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: solana-programs
        path: |
          target/deploy/*.so
          target/deploy/*-keypair.json
          target/idl/*.json

